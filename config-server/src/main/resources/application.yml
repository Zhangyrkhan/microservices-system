spring:
  application:
    name: config-server
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:../config-repo
server:
  port: 8888

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false